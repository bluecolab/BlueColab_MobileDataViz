// Legacy tab route kept as a no-op to avoid path collisions.
// We don't navigate here anymore. The center tab button pushes to /liveData (a Stack screen).
export default function LegacyCurrentDataTab() {
    return null;
}
